# ============================================================================
# CyberIntel — Python Dependencies
# ============================================================================
# Used by both:
#   - Vercel serverless functions (api/)
#   - GitHub Actions ingestion pipeline (ingestion/)
# ============================================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn>=0.30.0                      # Local dev server (not used on Vercel)

# --- Data Validation ---
pydantic>=2.0

# --- Database (Supabase) ---
supabase>=2.0.0                    # PostgREST client
asyncpg>=0.29.0                    # Direct SQL / pgvector queries

# --- AI / Gemini ---
google-generativeai>=0.8.0         # Gemini 2.0 Flash + embeddings

# --- Authentication ---
python-jose[cryptography]>=3.3.0   # JWT encoding/decoding
passlib[bcrypt]>=1.7.4             # Password hashing

# --- Ingestion (RSS + HTTP) ---
feedparser>=6.0.0                  # RSS/Atom feed parsing
httpx>=0.27.0                      # Async HTTP client
beautifulsoup4>=4.12.0             # HTML content extraction

# --- Utilities ---
python-dotenv>=1.0.0               # Load .env files for local development
