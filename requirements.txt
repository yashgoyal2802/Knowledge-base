# ============================================================================
# CyberIntel — Production Serverless Dependencies (Vercel)
# ============================================================================
# Lightweight dependencies for Vercel Python serverless functions (api/).
# Heavy ingestion dependencies (feedparser, bs4, etc.) are in requirements-ingestion.txt
# to ensure the bundle stays well below Vercel's 225 MB Lambda limit.
# ============================================================================

# --- Web Framework ---
fastapi>=0.115.0

# --- Data Validation ---
pydantic>=2.0

# --- Database (Supabase) ---
supabase>=2.0.0                    # PostgREST client
asyncpg>=0.29.0                    # Direct SQL / pgvector queries

# --- AI / Gemini ---
google-generativeai>=0.8.0         # Gemini 2.0 Flash + embeddings

# --- Authentication ---
python-jose>=3.3.0                 # JWT encoding/decoding (HS256)
passlib[bcrypt]>=1.7.4             # Password hashing

# --- HTTP Client ---
httpx>=0.27.0                      # Async HTTP client
