# ============================================================================
# CyberIntel — Production Serverless Dependencies (Vercel)
# ============================================================================
# Lightweight dependencies for Vercel Python serverless functions (api/).
# Heavy SDKs (google-generativeai/grpc, feedparser, bs4) are in requirements-ingestion.txt
# to ensure the Vercel Lambda bundle stays under ~95 MB (well below the 225 MB limit).
# ============================================================================

# --- Web Framework ---
fastapi>=0.115.0

# --- Data Validation ---
pydantic>=2.0

# --- Database (Supabase) ---
supabase>=2.0.0                    # PostgREST client
asyncpg>=0.29.0                    # Direct SQL / pgvector queries

# --- Authentication ---
python-jose>=3.3.0                 # JWT encoding/decoding (HS256)
passlib[bcrypt]>=1.7.4             # Password hashing

# --- HTTP Client (used for Supabase & direct Gemini REST calls) ---
httpx>=0.27.0                      # Async HTTP client
